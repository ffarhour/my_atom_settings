#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../analytics-node/bin/analytics" "$@"
  ret=$?
else 
  node  "$basedir/../analytics-node/bin/analytics" "$@"
  ret=$?
fi
exit $ret
